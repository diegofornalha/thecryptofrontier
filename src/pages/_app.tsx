import '../css/main.css';

// Desabilitar temporariamente importação do Visual Editing, se existir
// import { VisualEditing } from '@sanity/visual-editing';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp; 